## Desktop environment and window manager integration.

Modules in this directory define the system-level integration for desktop environments and window managers, including:

- Required system packages

- Session and login integration

- Portal backends

- Display manager hooks (if applicable)

These modules must not contain user dotfiles or UI preferences.
Per-user desktop configuration belongs in userland/.
